angular.module('spatentatenApp', ['ui.router','steelseries'])
    .controller('MainCtlr', ['$scope',
        function ($scope) {
        "use strict";
            $scope.soundIntensity = 33;
       
 
        }]
    );

